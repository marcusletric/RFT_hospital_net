/**
 * Created by Administrator on 2015.12.06..
 */
hospitalNet.constant('entityDefinitions', {});

hospitalNet.run(function($rootScope,entityDefinitions){
    $rootScope.entities = entityDefinitions;
});